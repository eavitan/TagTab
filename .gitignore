# Whitelist approach - ignore everything except what we want to keep

# Ignore everything by default
*

# Keep only these essential files and directories
!README.md
!.gitignore
!tagtab-ext/
!tagtab-ext/**

# Make sure to ignore OS and IDE files even if they're in kept directories
.DS_Store
.idea/
.vscode/
*.log
node_modules/